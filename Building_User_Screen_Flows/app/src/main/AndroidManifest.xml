<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Activitydemo"
        tools:targetApi="31">

        <!-- 🌿 Activity minh họa vòng đời -->
        <activity android:name=".LifecycleDemoActivity" />

        <!-- 💾 Activity minh họa lưu và khôi phục trạng thái -->
        <activity android:name=".SaveStateActivity" />

        <!-- ✉️ Activity truyền dữ liệu giữa Intent -->
        <activity android:name=".IntentInteractionActivity" />
        <activity android:name=".ResultActivity" />


        <!-- 🔹 Các Activity phụ cho Launch Mode demo -->
        <activity android:name=".LaunchModeActivity" />
        <activity android:name=".ProductListActivity" />
        <activity android:name=".ProductDetailActivity" android:launchMode="singleTop"/>
        <activity android:name=".CartActivity" android:launchMode="singleTask"/>
        <activity android:name=".CheckoutActivity" android:launchMode="singleInstance"/>
        <!-- 🏠 Activity chính (màn hình khởi động) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Activitydemo">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SecondActivity" />


    </application>

</manifest>
